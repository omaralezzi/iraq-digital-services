# Project Working Rules

## Git safety

- قبل أي تعديل كبير، افحص حالة Git باستخدام `git status`.
- إذا وُجدت تغييرات حالية غير محفوظة، حافظ عليها ولا تحذفها أو تستبدلها.
- قبل أي ميزة كبيرة أو إعادة هيكلة أو تعديل جوهري، أنشئ Safety Commit للحالة الحالية إذا كانت هناك تغييرات تستحق الحفظ.
- افحص التغييرات بعناية باستخدام `git diff` و`git diff --staged` قبل الحفظ.
- بعد انتهاء التعديل، شغّل الاختبارات أو عملية البناء المناسبة للمشروع.
- إذا ظهرت أخطاء بسبب التعديل، أصلحها قبل إنشاء الـ Commit النهائي.
- بعد نجاح التعديل، أنشئ Git Commit جديدًا باسم واضح يشرح ما تغيّر.
- لا تستخدم `git reset --hard`.
- لا تستخدم force push بأي صيغة.
- لا تحذف Git history أو تعيد كتابته.
- لا تنفّذ أي Push إلى GitHub أو أي remote إلا بطلب صريح من المستخدم.
- لا تضف ملفات `.env` أو مفاتيح API أو كلمات مرور أو رموز وصول أو أي Secrets إلى Git.
- تأكد قبل كل Commit من أن الملفات الحساسة مستثناة في `.gitignore` وغير موجودة ضمن الملفات المرحّلة.
- لا تغيّر أو تحذف أي ملف يخص المستخدم إلا لسبب مرتبط مباشرة بالمهمة الحالية.

## Required workflow for substantial changes

1. افحص `git status` وسجل آخر Commit.
2. احفظ التغييرات الحالية في Safety Commit عند الحاجة.
3. نفّذ التعديل المطلوب مع الحفاظ على عمل المستخدم الحالي.
4. شغّل `npm run verify`، أو الاختبار وعملية البناء المناسبين إذا تغيّرت أوامر المشروع.
5. راجع الـ diff النهائي والملفات المرحّلة، وتأكد من عدم وجود أسرار.
6. أنشئ Commit نهائيًا برسالة واضحة ومحددة.
7. لا تنفّذ Push ما لم يطلب المستخدم ذلك صراحةً.
