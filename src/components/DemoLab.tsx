"use client";

import { useState } from "react";
import type { Locale } from "@/src/content/siteSettings";
import { demoProjects } from "@/src/content/demoProjects";

export function DemoLab({ locale }: { locale: Locale }) {
  const [active, setActive] = useState<(typeof demoProjects)[number]["id"]>("store");
  const [pulse, setPulse] = useState(0);
  const ar = locale === "ar";
  const current = demoProjects.find(x => x.id === active) ?? demoProjects[0];
  const rows = ar ? ["طلب #104","طلب #103","طلب #102"] : locale === "de" ? ["Anfrage Nr. 104","Anfrage Nr. 103","Anfrage Nr. 102"] : ["Request #104","Request #103","Request #102"];
  const demoText = <T extends string>(arabic: T, english: T, german: T) => locale === "ar" ? arabic : locale === "de" ? german : english;
  return <section id="demos" className="section demos-section"><div className="container"><div className="section-heading split"><div><span className="eyebrow">{ar ? "جرّب الواجهة بنفسك" : locale === "de" ? "Probieren Sie die Oberfläche selbst aus" : "Try the interface yourself"}</span><h2>{ar ? "شاهد كيف تبدو الفكرة عندما تعمل فعليًا." : locale === "de" ? "Erleben Sie, wie sich die Idee im Einsatz anfühlt." : "See what the idea could feel like in action."}</h2></div><p>{ar ? "تجارب آمنة ببيانات غير حقيقية؛ لا تُنفّذ دفعًا أو طلبًا فعليًا." : locale === "de" ? "Sichere Vorschauen mit fiktiven Daten; es werden keine echten Bestellungen oder Zahlungen verarbeitet." : "Safe previews with fictional data; no real order or payment is processed."}</p></div><div className="demo-shell"><div className="demo-tabs" role="tablist" aria-label={ar ? "اختر نموذجًا" : locale === "de" ? "Demo auswählen" : "Choose a demo"}>{demoProjects.map(demo => <button type="button" role="tab" aria-selected={active === demo.id} onClick={() => setActive(demo.id)} key={demo.id}><span>{demo.icon}</span>{demoText(demo.titleAr, demo.titleEn, demo.titleDe)}</button>)}</div><div className="demo-stage" role="tabpanel"><div className="demo-window"><div className="demo-bar"><span></span><span></span><span></span><b>demo.local/{current.id}</b></div><div className="demo-dashboard"><aside><div className="demo-logo">{current.icon}</div><i></i><i></i><i></i><i></i></aside><div className="demo-content"><div className="demo-title"><div><small>{ar ? "واجهة تجريبية" : locale === "de" ? "Interaktive Vorschau" : "Interactive preview"}</small><h3>{demoText(current.titleAr, current.titleEn, current.titleDe)}</h3></div><button type="button" onClick={() => setPulse(x => x + 1)}>{ar ? "شغّل تفاعلًا" : locale === "de" ? "Interaktion starten" : "Run interaction"}<span>↯</span></button></div><div className="demo-metrics"><article><strong>{current.metric}</strong><span>{demoText(current.labelAr, current.labelEn, current.labelDe)}</span></article><article><strong>{pulse || "03"}</strong><span>{ar ? "تحديثات اليوم" : locale === "de" ? "heutige Aktualisierungen" : "updates today"}</span></article><article><strong>98%</strong><span>{ar ? "بيانات مكتملة" : locale === "de" ? "Daten vollständig" : "data complete"}</span></article></div><div className="demo-table"><div className="table-head"><span>{ar ? "الطلب" : locale === "de" ? "Anfrage" : "Request"}</span><span>{ar ? "الحالة" : locale === "de" ? "Status" : "Status"}</span><span>{ar ? "آخر تحديث" : locale === "de" ? "Aktualisiert" : "Updated"}</span></div>{rows.map((row, i) => <div className={pulse && i === 0 ? "table-row pulse" : "table-row"} key={row}><span><i>{current.icon}</i>{row}</span><span><b></b>{demoText(current.statusAr, current.statusEn, current.statusDe)}</span><span>{ar ? `قبل ${i + 2} د` : locale === "de" ? `vor ${i + 2} Min.` : `${i + 2}m ago`}</span></div>)}</div></div></div></div><div className="demo-disclaimer"><span>i</span><p><b>{ar ? "هذه واجهة تجريبية، وليست مشروعًا تابعًا لعميل حقيقي." : locale === "de" ? "Dies ist eine illustrative Demo – kein echtes Kundenprojekt." : "This is an illustrative demo—not a real client project."}</b><br />{ar ? "جميع الأسماء والبيانات خيالية، ولا تُرسل أي طلبات فعلية." : locale === "de" ? "Alle Namen und Daten sind fiktiv; es wird keine echte Aktion übermittelt." : "All names and data are fictional; no action is actually submitted."}</p></div></div></div></div></section>;
}
