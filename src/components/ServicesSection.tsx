import Link from "next/link";
import type { Locale } from "@/src/content/siteSettings";
import { services } from "@/src/content/services";

export function ServicesSection({ locale }: { locale: Locale }) { const ar = locale === "ar"; return <section id="services" className="section services-section"><div className="container"><div className="section-heading split"><div><span className="eyebrow">{ar ? "من الواجهة إلى العمليات" : locale === "de" ? "Von der Schnittstelle bis zum Betrieb" : "From interface to operations"}</span><h2>{ar ? "حلول رقمية تُبنى لتحقيق نتيجة، لا لمجرد مظهر جميل." : locale === "de" ? "Digitale Lösungen, die auf Ergebnisse ausgerichtet sind – nicht nur auf gutes Aussehen." : "Digital solutions built for outcomes—not just good looks."}</h2></div><p>{ar ? "لا تحتاج إلى معرفة الفرق بين الموقع والتطبيق ونظام CRM. أخبرنا بما تريد تحسينه، وسنحدّد الأداة المناسبة." : locale === "de" ? "Sie müssen nicht wissen, ob Sie eine Website, eine App oder ein CRM benötigen. Sagen Sie uns, was verbessert werden muss, und wir stellen Ihnen das richtige Tool zur Verfügung." : "You do not need to know whether you need a site, app or CRM. Tell us what needs improving and we will map the right tool."}</p></div><div className="services-grid">{services.map((service, i) => <article className="service-card" key={service.slug}><div className="card-top"><span className="service-icon" aria-hidden="true">{service.icon}</span><small>0{i + 1}</small></div><h3>{service.title[locale]}</h3><p>{service.short[locale]}</p><div className="mini-result"><b>{ar ? "النتيجة" : locale === "de" ? "Ergebnis" : "Outcome"}</b><span>{service.benefit[locale]}</span></div><Link href={`/${locale}/services/${service.slug}`}>{ar ? "استكشف الخدمة" : locale === "de" ? "Entdecken Sie den Service" : "Explore service"}<span>↗</span></Link></article>)}</div><div className="service-banner"><span>{ar ? "تحتاج إلى حل خاص؟" : locale === "de" ? "Benötigen Sie etwas Bestimmtes?" : "Need something specific?"}</span><p>{ar ? "نطوّر حلولًا تناسب طريقة عمل مشروعك، ويمكن البدء بنسخة صغيرة ثم توسيعها." : locale === "de" ? "Wir bauen auf Ihren Workflow auf. Fangen Sie klein an und erweitern Sie es, wenn es sich als nützlich erweist." : "We build around your workflow. Start small and expand when it proves useful."}</p><Link className="button ghost" href={`/${locale}#project-wizard`}>{ar ? "احصل على تقييم أولي" : locale === "de" ? "Holen Sie sich eine erste Einschätzung" : "Get an initial assessment"}</Link></div></div></section>; }
