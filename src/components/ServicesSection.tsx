import Link from "next/link";
import type { Locale } from "@/src/content/siteSettings";
import { services } from "@/src/content/services";

export function ServicesSection({ locale }: { locale: Locale }) { const ar = locale === "ar"; return <section id="services" className="section services-section"><div className="container"><div className="section-heading split"><div><span className="eyebrow">{ar ? "من الواجهة إلى العمليات" : "From interface to operations"}</span><h2>{ar ? "حلول رقمية تُبنى لتحقيق نتيجة، لا لمجرد مظهر جميل." : "Digital solutions built for outcomes—not just good looks."}</h2></div><p>{ar ? "لا تحتاج إلى معرفة الفرق بين الموقع والتطبيق ونظام CRM. أخبرنا بما تريد تحسينه، وسنحدّد الأداة المناسبة." : "You do not need to know whether you need a site, app or CRM. Tell us what needs improving and we will map the right tool."}</p></div><div className="services-grid">{services.map((service, i) => <article className="service-card" key={service.slug}><div className="card-top"><span className="service-icon" aria-hidden="true">{service.icon}</span><small>0{i + 1}</small></div><h3>{service.title[locale]}</h3><p>{service.short[locale]}</p><div className="mini-result"><b>{ar ? "النتيجة" : "Outcome"}</b><span>{service.benefit[locale]}</span></div><Link href={`/${locale}/services/${service.slug}`}>{ar ? "استكشف الخدمة" : "Explore service"}<span>↗</span></Link></article>)}</div><div className="service-banner"><span>{ar ? "تحتاج إلى حل خاص؟" : "Need something specific?"}</span><p>{ar ? "نطوّر حلولًا تناسب طريقة عمل مشروعك، ويمكن البدء بنسخة صغيرة ثم توسيعها." : "We build around your workflow. Start small and expand when it proves useful."}</p><Link className="button ghost" href={`/${locale}#project-wizard`}>{ar ? "احصل على تقييم أولي" : "Get an initial assessment"}</Link></div></div></section>; }
